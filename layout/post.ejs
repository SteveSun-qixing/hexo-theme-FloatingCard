<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> | <%= config.title %></title>
    <%- css('css/style.css') %>
</head>
<body class="article-page">
    <div class="content-card">
        <h1 class="article-title"><%= page.title %></h1>
        <div class="article-meta">
            <% if (page.date) { %>
                <span class="article-date"><%- date(page.date, config.date_format) %></span>
            <% } %>
        </div>
        <article class="article-content image-content">
            <%- page.content %>
        </article>
    </div>
    <%- js('js/script.js') %>
</body>
</html>