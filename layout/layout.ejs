<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> | <%= config.title %></title>
    <%- css('css/style.css') %>  <!-- 引入样式文件 -->
</head>
<body>
    <div id="card-container"></div>  <!-- 卡片容器 -->
    <%- body %>  <!-- 页面主体内容 -->
    <script>
        // 将文章数据传递给前端JavaScript
        window.hexoPostsData = <%- JSON.stringify(site.posts.data.map(post => ({
            title: post.title,
            date: post.date,
            path: url_for(post.path)
        }))) %>;
    </script>
    <%- js('js/script.js') %>  <!-- 引入JavaScript文件 -->
</body>
</html>